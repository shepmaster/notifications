<% text = notification.read ? 'read' : 'unread' %>

<turbo-frame id="moocow-<%= notification.id%>">
  <%= button_to("#{text} #{notification.id}", notification_path(notification), method: :patch, form: { data: { turbo: false } }) %>
</turbo-frame>
