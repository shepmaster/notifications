<% text = notification.read ? 'read' : 'unread' %>

<%= button_to("#{text} #{notification.id}", notification_path(notification), method: :patch, form: { data: { turbo: false, controller: 'fixup-csrf' } }) %>
